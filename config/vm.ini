[DEFAULT]
name = Test
description = Just a VM for Testing
memory = 4096
vcpus = 4
path = /var/lib/libvirt/images/centos8    # qcow/raw image for the VM data 
iso = /var/lib/libvirt/images/boot.iso    # Linux boot iso
kickstart = http://                       # Add IP then path of anaconda file in the directory python runs in